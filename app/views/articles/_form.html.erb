<%= form_for(@article) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.text_field :content %>

  <% if @article.new_record? %>
    <%= f.submit 'Create!' %>
  <% else %>
    <%= f.submit 'Update!' %>
  <% end %>
<% end %>
