<h1><%= @article.title %></h1>
<p><%= @article.content %></p>

<%= link_to 'Edit article', edit_article_path %>

<%= link_to 'Return to all articles', articles_path %>

<%= link_to 'Delete article', article_path(@article), method: :delete,
data: { confirm: "Delete #{@article.title}?" }%>
